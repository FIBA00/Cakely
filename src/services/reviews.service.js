// Patisserie Postcard: only verified, persisted customer reviews are eligible for display.
import { wait } from "./api.js";

export async function getCakeReviews(_cakeId) {
  // Replace with GET /cakes/:cakeId/reviews once verified-order reviews are available.
  return wait([]);
}
